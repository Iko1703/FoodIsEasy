package com.example.FoodIsEasy.model.enums;

/**
 * Роль блюда в рамках одного приёма пищи (метка для подбора нескольких позиций без дубля «суп + суп»).
 */
public enum MealRole {
    BREAKFAST,
    SOUP,
    MAIN,
    SALAD,
    SIDE,
    DESSERT,
    SNACK
}
